~
#var = $env.envExec._var
#varname = $env.envExec._varname

#filter = {}
Arr_Str#x = #1;
@if(x){
 @each _ k x{
  filter[k] = 1
 }
}

@each k v #0.blockStateDef.schema() {
 @if(!filter[k]){
  ~~:call(var, [varname(k), v], $env)~
~
 }
}
~