~
#var = scopeGet($env.envExec, "var")
#filter = {}
Arr#x = #1;
@if(x){
 @each _ v x{
  filter[v] = 1
 }
}

@each k v #0.blockStateDef.schema() {
 @if(!filter.hasKey(k)){
  ~~:call(var, [k, v], $env)~
~
 }
}
~