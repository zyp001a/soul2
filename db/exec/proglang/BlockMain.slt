~
Str#mainpath = $env.envGlobal.$blockmain.blockPath
#mainflag = getName($env.envExec)
#maindir = mainpath + "." + mainflag
mkdirAll(maindir)

##$filelist = {a:"1"}
@each k v $filelist{
 @if(isStr(v)){
  log(v)
 }
}
~