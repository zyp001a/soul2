-@env go -> go
 state1 -> main
	when Call keep main  	 
  when exec flag1-> go -> 
	when call 
	 ->callx -all main

speed up!
make CallId
dynamic allow arr length
interface to type
setx
//isdef flag
auto gen soul "def" func to go func
soul method grammar
pointer, addr
func type check